(function () {
  const backToTopMount = document.getElementById('site-back-to-top');
  if (!backToTopMount) return;

  backToTopMount.innerHTML = '<button id="back-to-top" title="Back to top">&uarr;</button>';
})();
